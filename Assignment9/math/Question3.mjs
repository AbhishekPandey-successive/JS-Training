import {addition,subtraction} from './math.mjs'

const sumAnswer=addition(2,3);
const subAnswer=subtraction(10,5);
console.log(`sum of two number is ${sumAnswer}`)
console.log(`subtraction of two number is ${subAnswer}`)
